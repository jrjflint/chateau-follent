# Environment variables consumed by docker-compose for the cloudflared service.
# Copy this file to cloudflared/.env and store the populated version in a secure
# secrets manager rather than committing it to Git. Rotate the values whenever
# tunnel credentials for Lord James Follent change.

CLOUDFLARED_TUNNEL_TOKEN=
CLOUDFLARED_TUNNEL_UUID=
# Host path pointing to the downloaded credentials JSON. Keep the actual file in
# a secure location outside of the repository and mount it at runtime.
CLOUDFLARED_CREDENTIALS_FILE_HOST=
# Container path matching the credentials-file entry in config.yml.
CLOUDFLARED_CREDENTIALS_FILE_CONTAINER=/etc/cloudflared/tunnel-credentials.json
